= form_for ([@blog, @blog.comments.build]) do |f|
  .field
    = f.label :poster
    = f.text_field :poster
  .field
    = f.label :description
    = f.text_area :description
  .actions
    = f.submit 'Save'
